# 🚀 Gate.io Trading Bot - دليل الاستخدام

## نظرة عامة

بوت تداول ذكي يربط حسابك على Gate.io ويتداول تلقائياً باستخدام الذكاء الاصطناعي.

### ✨ المميزات

- 🤖 **تداول ذكي بالـ AI**: يستخدم GPT-4 أو Claude لاتخاذ قرارات التداول
- 💰 **إدارة محفظة تلقائية**: يدير رصيدك ويوزع الاستثمارات
- 📊 **تحليل السوق الحي**: يحلل الأسعار والاتجاهات في الوقت الفعلي
- 🔄 **تداول مستمر**: يمكن تشغيله 24/7 مع فترات زمنية محددة
- 📝 **سجلات كاملة**: يحفظ كل الصفقات والقرارات

### 💱 العملات المدعومة

- BTC/USDT (Bitcoin)
- ETH/USDT (Ethereum)
- SOL/USDT (Solana)
- BNB/USDT (Binance Coin)

---

## 📋 المتطلبات

### 1. حساب Gate.io

- سجل على [Gate.io](https://www.gate.io/signup)
- فعّل التداول الفوري (Spot Trading)
- احصل على API Keys من [صفحة API](https://www.gate.io/myaccount/apiv4keys)

### 2. مفاتيح API

عند إنشاء API Key على Gate.io:
- ✅ فعّل صلاحية **Spot Trading**
- ✅ فعّل صلاحية **Read Only** (للاستعلام)
- ⚠️ **مهم**: احفظ API Secret في مكان آمن (يظهر مرة واحدة فقط)

### 3. OpenAI API Key

- احصل على مفتاح من [OpenAI Platform](https://platform.openai.com/api-keys)
- أو استخدم أي خدمة متوافقة مع OpenAI API

---

## ⚙️ التثبيت والإعداد

### الخطوة 1: تثبيت المتطلبات

```bash
# تثبيت المكتبات المطلوبة
pip install -r requirements.txt

# أو تثبيت يدوي
pip install requests python-dotenv langchain langchain-openai langchain-mcp-adapters
```

### الخطوة 2: إعداد ملف .env

```bash
# انسخ ملف المثال
cp .env.example .env

# افتح .env وأضف مفاتيحك
nano .env
```

أضف المفاتيح التالية في `.env`:

```bash
# OpenAI API
OPENAI_API_KEY="sk-your-openai-key-here"
OPENAI_API_BASE="https://api.openai.com/v1"  # اختياري

# Gate.io API
GATE_API_KEY="your-gate-api-key-here"
GATE_API_SECRET="your-gate-api-secret-here"

# Jina Search (اختياري - للبحث عن الأخبار)
JINA_API_KEY="your-jina-key-here"
```

---

## 🚀 التشغيل

### الطريقة 1: سكريبت سريع (موصى به)

```bash
# تشغيل مباشر
python3 run_gate_trader.py
```

أو باستخدام Bash:

```bash
bash scripts/run_gate_trader.sh
```

### الطريقة 2: استخدام main.py

```bash
# تشغيل باستخدام ملف الإعدادات
python3 main.py configs/gate_config.json
```

---

## 📖 دليل الاستخدام

### عند تشغيل البوت، ستظهر لك القائمة:

```
Choose an option:
1. Run single trading session      # جلسة تداول واحدة
2. Run continuous trading           # تداول مستمر
3. Check market prices only         # فقط عرض الأسعار
4. Exit                             # خروج
```

### الخيار 1: جلسة تداول واحدة

- يحلل السوق مرة واحدة
- يتخذ قرارات الشراء/البيع
- يعرض النتائج ويتوقف

### الخيار 2: تداول مستمر

- يعمل 24/7
- يحلل السوق كل ساعة (أو حسب الفترة المحددة)
- يتداول تلقائياً
- اضغط Ctrl+C للإيقاف

### الخيار 3: عرض الأسعار فقط

- يعرض أسعار العملات الحالية
- لا يقوم بأي تداول
- مفيد للمراقبة

---

## ⚙️ الإعدادات المتقدمة

### تعديل ملف `configs/gate_config.json`:

```json
{
  "agent_type": "GateAgent",
  "models": [
    {
      "name": "gpt-4o",
      "basemodel": "openai/gpt-4o",
      "signature": "gate-trader-gpt4o",
      "enabled": true
    }
  ],
  "agent_config": {
    "max_steps": 30,              // عدد خطوات التفكير القصوى
    "initial_cash": 1000.0,       // الرصيد الافتراضي (للتتبع)
    "trading_interval_minutes": 60 // الفترة بين الجلسات (بالدقائق)
  },
  "gate_config": {
    "trading_pairs": [
      "BTC_USDT",
      "ETH_USDT",
      "SOL_USDT",
      "BNB_USDT"
    ]
  }
}
```

### تغيير نموذج الذكاء الاصطناعي:

يمكنك استخدام:
- `gpt-4o` (موصى به)
- `gpt-4`
- `claude-3.7-sonnet`
- `deepseek/deepseek-chat-v3.1`
- أي نموذج متوافق مع OpenAI API

---

## 📊 السجلات والتقارير

### مواقع الملفات:

```
data/gate_agent_data/
├── gate-trader-live/
│   ├── position/          # سجلات المراكز
│   ├── log/              # سجلات الجلسات
│   └── trades/           # سجلات الصفقات
```

### سجل الصفقات:

```json
{
  "timestamp": "2026-01-30T10:30:00",
  "pair": "BTC_USDT",
  "side": "buy",
  "amount": 0.001,
  "price": 45000.0,
  "type": "market"
}
```

---

## 🔒 الأمان

### ⚠️ نصائح مهمة:

1. **لا تشارك API Keys أبداً**
2. **استخدم IP Whitelist على Gate.io**
3. **ابدأ برصيد صغير للتجربة**
4. **راقب البوت بانتظام**
5. **ضع حدود للخسارة**

### حماية المفاتيح:

```bash
# تأكد أن .env في .gitignore
echo ".env" >> .gitignore

# لا ترفع .env على GitHub
git rm --cached .env
```

---

## 🛠️ استكشاف الأخطاء

### خطأ: "Gate.io API credentials required"

```bash
# تأكد من وجود المفاتيح في .env
cat .env | grep GATE_API
```

### خطأ: "API request failed"

- تحقق من صحة API Key و Secret
- تأكد من تفعيل صلاحيات Spot Trading
- تحقق من اتصال الإنترنت

### خطأ: "OpenAI API key not found"

```bash
# أضف المفتاح في .env
echo 'OPENAI_API_KEY="sk-your-key"' >> .env
```

---

## 📈 استراتيجية التداول

البوت يستخدم AI لـ:

1. **تحليل فني**: يدرس الأسعار والاتجاهات
2. **إدارة مخاطر**: لا يخاطر بأكثر من 10% في صفقة
3. **تنويع**: يوزع الاستثمار على عدة عملات
4. **Stop-Loss**: يضع حدود للخسارة تلقائياً

---

## 🤝 المساهمة

لإضافة ميزات جديدة:

1. Fork المشروع
2. أنشئ branch جديد
3. أضف التعديلات
4. أرسل Pull Request

---

## 📞 الدعم

- **Issues**: [GitHub Issues](https://github.com/HKUDS/AI-Trader/issues)
- **Discussions**: [GitHub Discussions](https://github.com/HKUDS/AI-Trader/discussions)

---

## ⚖️ إخلاء المسؤولية

⚠️ **تحذير مهم**:

- هذا البوت للأغراض التعليمية والتجريبية
- التداول في العملات الرقمية ينطوي على مخاطر عالية
- قد تخسر كل رأس المال المستثمر
- استخدم على مسؤوليتك الخاصة
- ننصح بالبدء برصيد صغير للتجربة

---

## 📄 الترخيص

MIT License - استخدم بحرية مع ذكر المصدر

---

## 🎉 ابدأ الآن!

```bash
# تثبيت
pip install -r requirements.txt

# إعداد
cp .env.example .env
# أضف مفاتيحك في .env

# تشغيل
python3 run_gate_trader.py
```

**يلا نربح! 🚀💰**
