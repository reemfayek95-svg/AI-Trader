# 🚀 دليل إعداد MT5 Copy Trading Bot

## 📋 المتطلبات الأساسية

### 1️⃣ حساب MT5
- حساب تداول MT5 نشط
- رقم الحساب، الباسورد، واسم السيرفر
- رصيد كافٍ للتداول (يُنصح بـ $100 على الأقل)

### 2️⃣ منصة MetaTrader 5
- تحميل MT5 Desktop (Windows/Mac/Linux)
- أو استخدام MT5 عبر المتصفح
- **⚠️ تفعيل Algorithmic Trading في الإعدادات (مهم جداً!)**

---

## 🔥 تنبيه مهم: تفعيل التداول الآلي

**قبل أي حاجة، لازم تفعّلي التداول الآلي:**

### في MT5:
1. `Tools` → `Options`
2. تاب `Expert Advisors`
3. فعّلي ✅:
   - **Allow automated trading**
   - Allow DLL imports
   - Allow WebRequest for listed URL
4. اضغطي `OK`

### في الشريط العلوي:
- اضغطي زر **AutoTrading**
- لازم يكون **أخضر** 🟢 مش أحمر 🔴

**لو مش مفعّل هيطلعلك Error 133** ❌

---

## ⚙️ خطوات الإعداد

### الخطوة 1: تثبيت المكتبات المطلوبة

```bash
pip install MetaTrader5 pandas numpy python-dotenv
```

### الخطوة 2: إعداد ملف البيئة (.env)

1. انسخ ملف `.env.example` إلى `.env`:
```bash
cp .env.example .env
```

2. افتح ملف `.env` وأضف بيانات حساب MT5:
```env
# بيانات حساب MT5
MT5_ACCOUNT=123456789          # رقم حسابك
MT5_PASSWORD=YourPassword123   # باسورد الحساب
MT5_SERVER=Broker-Server       # اسم السيرفر (مثلاً: ICMarkets-Demo)

# اختياري: نسبة عمولة الأداء
PERFORMANCE_FEE=0.05           # 5% من الأرباح
```

### الخطوة 3: التشغيل

#### طريقة سريعة (مع اختيار الاستراتيجية):
```bash
bash RUN_MT5_BOT.sh
```

#### طريقة يدوية:
```bash
# استراتيجية بسيطة
python mt5_copy_trading.py

# أو استراتيجية متقدمة
python -c "
from mt5_copy_trading import MT5CopyTrader
from mt5_advanced_strategy import get_strategy
import os

trader = MT5CopyTrader(
    account=int(os.getenv('MT5_ACCOUNT')),
    password=os.getenv('MT5_PASSWORD'),
    server=os.getenv('MT5_SERVER')
)
trader.connect()
strategy = get_strategy('trend')  # أو 'scalping', 'breakout', 'ml'
trader.auto_trade_loop(strategy, interval=60)
"
```

---

## 📊 الاستراتيجيات المتاحة

### 1. **Trend Following** (متابعة الترند) ⭐ موصى به
- **الوصف**: يتابع اتجاه السوق باستخدام Moving Averages و RSI
- **الأزواج**: EURUSD, GBPUSD, USDJPY
- **الإطار الزمني**: 15 دقيقة
- **المخاطرة**: 2% من الرصيد لكل صفقة
- **مناسب لـ**: المبتدئين والأسواق الترندية

**استخدام**:
```python
from mt5_advanced_strategy import get_strategy
strategy = get_strategy('trend')
```

### 2. **Scalping** (سكالبينج) ⚡
- **الوصف**: صفقات سريعة تستهدف أرباح صغيرة متكررة
- **الأزواج**: EURUSD, GBPUSD
- **الإطار الزمني**: 5 دقائق
- **المخاطرة**: 1% من الرصيد لكل صفقة
- **مناسب لـ**: الأسواق الجانبية والمتداولين النشطين

**استخدام**:
```python
strategy = get_strategy('scalping')
```

### 3. **Breakout** (اختراق المستويات) 🎯
- **الوصف**: يتداول على كسر مستويات الدعم والمقاومة
- **الأزواج**: EURUSD, XAUUSD (الذهب)
- **الإطار الزمني**: ساعة واحدة
- **المخاطرة**: 2.5% من الرصيد لكل صفقة
- **مناسب لـ**: الأخبار والأحداث الاقتصادية

**استخدام**:
```python
strategy = get_strategy('breakout')
```

### 4. **ML Strategy** (تعلم آلي) 🤖
- **الوصف**: استراتيجية بسيطة تستخدم التعلم الآلي للتنبؤ
- **الأزواج**: EURUSD
- **الإطار الزمني**: 15 دقيقة
- **المخاطرة**: 1.5% من الرصيد لكل صفقة
- **مناسب لـ**: التجريب والتطوير

**استخدام**:
```python
strategy = get_strategy('ml')
```

---

## 🔧 إعدادات متقدمة

### تخصيص المخاطرة
```python
trader = MT5CopyTrader(
    account=your_account,
    password=your_password,
    server=your_server,
    performance_fee=0.05,  # 5% عمولة أداء
    max_leverage=200       # رافعة مالية 1:200
)
```

### تعديل فترة التحديث
```python
trader.auto_trade_loop(
    strategy_func=strategy,
    interval=30,     # تحديث كل 30 ثانية
    max_trades=5     # أقصى 5 صفقات متزامنة
)
```

### مراقبة الأداء
البوت يسجل كل صفقة ويطبع:
- ✅ فتح/إغلاق الصفقات
- 💰 الأرباح والخسائر
- 📊 عدد الصفقات المفتوحة
- 💵 عمولة الأداء المحسوبة

---

## ⚠️ تحذيرات ونصائح مهمة

### 🔴 المخاطر
- التداول الآلي ينطوي على مخاطر خسارة رأس المال
- ابدأ بحساب تجريبي (Demo) أولاً
- لا تستثمر أموالاً لا تستطيع خسارتها
- راقب البوت بانتظام

### ✅ نصائح للنجاح
1. **اختبر على Demo أولاً**: جرّب الاستراتيجيات على حساب تجريبي لمدة أسبوع على الأقل
2. **ابدأ صغيراً**: استخدم أحجام صفقات صغيرة (0.01 لوت) في البداية
3. **راقب السوق**: لا تترك البوت يعمل دون مراقبة لفترات طويلة
4. **استخدم Stop Loss**: جميع الاستراتيجيات تستخدم SL تلقائياً
5. **تنويع الاستراتيجيات**: يمكنك تشغيل عدة بوتات بحسابات مختلفة

### 🛠️ استكشاف الأخطاء

#### خطأ: "❌ فشل تهيئة MT5"
- **الحل**: تأكد من تثبيت MT5 على جهازك
- أو: أعد تشغيل منصة MT5

#### خطأ: "❌ فشل تسجيل الدخول"
- **الحل**: تحقق من بيانات الحساب في `.env`
- تأكد من اسم السيرفر الصحيح
- تأكد من السماح بالتداول الآلي في إعدادات MT5

#### خطأ: "غير متصل بالإنترنت"
- **الحل**: تحقق من اتصال الإنترنت
- افتح MT5 يدوياً وتحقق من الاتصال

---

## 📈 مثال على النتائج المتوقعة

### استراتيجية Trend Following (أسبوع واحد)
```
💰 الرصيد الأولي: $1,000.00
📊 عدد الصفقات: 15
✅ صفقات رابحة: 10 (66%)
❌ صفقات خاسرة: 5 (34%)
💵 صافي الربح: $87.50
📈 العائد: +8.75%
💰 عمولة الأداء (5%): $4.38
```

---

## 🔗 روابط مفيدة

- [MetaTrader 5 Official](https://www.metatrader5.com/)
- [MT5 Python API Docs](https://www.mql5.com/en/docs/python_metatrader5)
- [تحميل MT5](https://download.mql5.com/cdn/web/metaquotes.software.corp/mt5/mt5setup.exe)

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من ملف `.env`
2. تأكد من تثبيت جميع المكتبات
3. جرّب على حساب Demo أولاً
4. افتح Issue على GitHub

---

## 🚀 ابدأ الآن!

```bash
# خطوة واحدة فقط:
bash RUN_MT5_BOT.sh
```

**وفقك الله وكتر فلوسك! 💰**
