# 🔧 حل مشكلة ERR_TRADE_DISABLED (133)

## المشكلة
```
retcode=133 (ERR_TRADE_DISABLED) - Trading is disabled
```

---

## ✅ الحل السريع - 3 خطوات

### 1️⃣ تفعيل التداول الآلي في MT5

**على Windows/Mac:**
1. افتح MT5
2. اضغط `Tools` → `Options`
3. تاب `Expert Advisors`
4. فعّل ✅:
   - **Allow automated trading**
   - Allow DLL imports
   - Allow WebRequest for listed URL

5. اضغط `OK`

**الصورة:**
```
┌─────────────────────────────────────┐
│ [✅] Allow automated trading        │
│ [✅] Allow DLL imports              │
│ [✅] Allow WebRequest...            │
└─────────────────────────────────────┘
```

---

### 2️⃣ تفعيل Auto Trading في Toolbar

**في واجهة MT5:**
- اضغط على زر **"AutoTrading"** في شريط الأدوات
- لازم يكون أخضر 🟢 مش أحمر 🔴

**مكان الزر:**
```
File  View  Insert  Charts  Tools  Window  Help
[📊] [📈] [🤖 AutoTrading] ← لازم أخضر
```

---

### 3️⃣ فحص سماحيات الحساب

**احتمال الحساب Demo/Real معطّل فيه EAs:**

#### ✅ إذا كنتي على **Demo:**
- روحي على موقع البروكر
- اطلبي حساب Demo جديد
- **فعّلي Expert Advisors allowed** في الطلب

#### ✅ إذا كنتي على **Real:**
- كلّمي الدعم الفني للبروكر
- اطلبي تفعيل **Algorithmic Trading**
- بعض البروكرز يحتاج موافقة يدوية

---

## 🧪 اختبار الحل

### بعد ما تعملي الخطوات:

```bash
python3 test_mt5_fixed.py
```

**لو الكود ده اشتغل، يبقى تمام:**

```python
import MetaTrader5 as mt5

# اتصال
mt5.initialize()
mt5.login(account=123456, password="pass", server="Broker-Demo")

# فحص التداول
terminal = mt5.terminal_info()
print(f"التداول الآلي: {'✅ مفعّل' if terminal.trade_allowed else '❌ معطّل'}")

# محاولة فتح صفقة تجريبية
symbol = "EURUSD"
mt5.symbol_select(symbol, True)

request = {
    "action": mt5.TRADE_ACTION_DEAL,
    "symbol": symbol,
    "volume": 0.01,
    "type": mt5.ORDER_TYPE_BUY,
    "price": mt5.symbol_info_tick(symbol).ask,
    "deviation": 20,
    "magic": 123,
    "comment": "Test",
    "type_time": mt5.ORDER_TIME_GTC,
    "type_filling": mt5.ORDER_FILLING_IOC,
}

result = mt5.order_send(request)
print(f"النتيجة: {result.retcode}")

if result.retcode == 10009:  # TRADE_RETCODE_DONE
    print("✅ نجح! التداول شغّال")
else:
    print(f"❌ لسه في مشكلة: {result.comment}")

mt5.shutdown()
```

---

## 🚨 لو لسه مش شغّال

### فحوصات إضافية:

#### 1. الحساب Demo/Real صحيح؟
```python
account = mt5.account_info()
print(f"نوع الحساب: {account.trade_mode}")
# 0 = Demo, 1 = Contest, 2 = Real
```

#### 2. السيمبول متاح للتداول؟
```python
symbol_info = mt5.symbol_info("EURUSD")
print(f"التداول متاح: {symbol_info.trade_mode}")
# لازم يكون > 0
```

#### 3. السوق مفتوح؟
```python
if result.retcode == 134:
    print("❌ السوق مقفول - استنّي لحد ما يفتح")
```

#### 4. الحجم صحيح؟
```python
symbol = mt5.symbol_info("EURUSD")
print(f"أقل حجم: {symbol.volume_min}")
print(f"أقصى حجم: {symbol.volume_max}")
# استخدمي 0.01 أو أكبر
```

---

## 📞 لو فشل كل حاجة

### كلّمي البروكر بتاعك:

**الأسئلة المهمة:**
1. ✅ هل الحساب بتاعي يسمح بـ Expert Advisors؟
2. ✅ هل في قيود على API Trading؟
3. ✅ محتاج أي موافقات إضافية؟

**بروكرز معروفة بدعم MT5 API:**
- XM
- Pepperstone
- IC Markets
- Admiral Markets
- FXCM

---

## ✅ ملخص سريع

| الخطوة | الحل |
|--------|------|
| 1️⃣ | Tools > Options > Expert Advisors > Allow automated trading |
| 2️⃣ | اضغط زر AutoTrading (لازم أخضر) |
| 3️⃣ | تأكدي الحساب يسمح بـ EAs |
| 4️⃣ | جربي الكود التجريبي فوق |

---

## 💡 نصيحة أخيرة

**لو عايزة تضمني الموضوع يشتغل:**
1. استخدمي **حساب Demo أول** عشان تتعلمي
2. فعّلي **Allow automated trading** قبل أي حاجة
3. جربي على **EURUSD** لأنه الأكتر توفر
4. ابدأي بـ **حجم صغير** (0.01 lot)

---

**تمت معالجة الكود تلقائيًا ✅**
البوت دلوقتي بيفحص ويدّيكي الحلول لما يحصل Error 133.
