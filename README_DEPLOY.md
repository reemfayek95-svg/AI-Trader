# 🚀 AI-Trader - دليل التشغيل السريع

## ✅ تم التثبيت بنجاح!

### 📋 ما تم إنجازه:

1. ✅ **Python 3.11** - تم التثبيت
2. ✅ **المكتبات** - تم تثبيت جميع المتطلبات
3. ✅ **MCP Services** - 5 خدمات تعمل الآن:
   - Math Service (Port 8000)
   - Search Service (Port 8001)
   - Trade Service (Port 8002)
   - Price Service (Port 8003)
   - Crypto Service (Port 8005)
4. ✅ **ملف .env** - تم إنشاؤه

---

## 🎯 الخطوات التالية:

### 1️⃣ **إضافة API Keys**
افتح ملف `.env` وأضف المفاتيح الخاصة بك:

```bash
nano .env
```

**المطلوب:**
- `OPENAI_API_KEY` - للنماذج الذكية
- `ALPHAADVANTAGE_API_KEY` - لبيانات الأسهم
- `JINA_API_KEY` - للبحث (اختياري)
- `TUSHARE_TOKEN` - للأسهم الصينية (اختياري)

### 2️⃣ **تشغيل AI Trading Agent**

```bash
# للأسهم الأمريكية (NASDAQ 100)
python3.11 main.py configs/default_config.json

# للأسهم الصينية (SSE 50)
python3.11 main.py configs/astock_config.json

# للعملات الرقمية
python3.11 main.py configs/default_crypto_config.json
```

### 3️⃣ **تحضير البيانات** (إذا لزم الأمر)

```bash
# بيانات الأسهم الأمريكية
cd data && python3.11 get_daily_price.py && python3.11 merge_jsonl.py

# بيانات الأسهم الصينية
cd data/A_stock && python3.11 get_daily_price_tushare.py && python3.11 merge_jsonl_tushare.py

# بيانات العملات الرقمية
cd data/crypto && python3.11 get_daily_price_crypto.py && python3.11 merge_crypto_jsonl.py
```

---

## 🛠️ أوامر مفيدة:

```bash
# إعادة تشغيل الخدمات
./deploy.sh

# فحص الخدمات
ps aux | grep "python3.11.*tool_" | grep -v grep

# إيقاف الخدمات
pkill -f "python3.11.*tool_"

# عرض السجلات
tail -f logs/*.log
```

---

## 📊 الوصول للواجهة:

```bash
# تشغيل الواجهة
cd docs && python3.11 -m http.server 8888
# ثم افتح: http://localhost:8888
```

---

## ⚙️ الإعدادات:

- **الإعدادات الافتراضية**: `configs/default_config.json`
- **رأس المال الابتدائي**: $10,000 (أمريكي) / ¥100,000 (صيني) / 50,000 USDT (عملات رقمية)
- **فترة التداول**: يمكن تعديلها في ملف الإعدادات

---

## 🆘 المشاكل الشائعة:

### الخدمات لا تعمل؟
```bash
# تحقق من السجلات
cat logs/*.log

# أعد التشغيل
pkill -f "python3.11.*tool_"
python3.11 agent_tools/start_mcp_services.py &
```

### خطأ في API Keys؟
تأكد من إضافة المفاتيح في ملف `.env`

### خطأ في البيانات؟
قم بتحميل البيانات أولاً باستخدام السكريبتات في مجلد `data/`

---

## 📚 المزيد من المعلومات:

- **الوثائق الكاملة**: `README.md`
- **الوثائق الصينية**: `README_CN.md`
- **الموقع الرسمي**: https://ai4trade.ai

---

**🎉 جاهز للتداول! حظاً موفقاً!**
